# MOD 2: DOCKER Configuration Template
# Copy to .env.docker and update with your values

# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_USER=voting_docker_user
DB_PASSWORD=docker_secure_password_change_this
DB_NAME=voting_app_docker

# Deployment Mode
DEPLOYMENT_MODE=docker

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Security
CORS_ORIGINS=["http://localhost", "http://localhost:80"]
